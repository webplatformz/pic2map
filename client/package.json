{
  "name": "pic2map-client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "ajv": "^6.5.0",
    "mapbox-gl": "^0.44.0",
    "react": "^16.4.0",
    "react-dom": "^16.4.0",
    "react-mapbox-gl": "^3.8.0",
    "react-router-dom": "^4.2.2",
    "react-scripts": "1.1.4"
  },
  "proxy": {
    "/api/*": {
      "target": "http://localhost:8080"
    }
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --coverage --env=jsdom",
    "test:ci": "react-scripts test --coverage --env=jsdom",
    "eject": "react-scripts eject"
  }
}
